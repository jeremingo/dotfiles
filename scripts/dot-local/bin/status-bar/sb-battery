#!/bin/sh

capacity=`cat /sys/class/power_supply/BAT0/capacity`

if [ $( grep 'Charging\|Full' /sys/class/power_supply/BAT0/status ) ]; then
  icon="\U000f0084"
elif ((capacity < 20)); then
  icon="\U000f0083"
else
  icon="\U000f007f"
fi

echo -e "$capacity$icon"
